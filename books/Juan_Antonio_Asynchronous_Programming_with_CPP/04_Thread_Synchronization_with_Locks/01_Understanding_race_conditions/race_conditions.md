
# Understanding race conditions

A race condition happens when the outcome of running a program depends on the sequence in which its instructions are executed.

This scenario involves two threads, t1 and t2, that run concurrently and modify the same variable, which is essentially some memory region.


